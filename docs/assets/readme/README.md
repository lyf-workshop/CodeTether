# README 图片说明

本目录仅保存为 GitHub 项目介绍逐张检查过的静态截图，不是完整验证证据目录。图片直接复制自隔离测试截图，未修改界面或制造就绪状态。原始失败、通过及后续验证记录均保留在忽略的 `output/` 目录，不随本目录发布。

| 图片                         | 来源与适用范围                                                                                                              |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `conversation-workspace.png` | Phase 5A 的早期工作区截图：隔离项目 `workspace`，Codex / Claude Code 空会话。只展示布局，不是当前版本的执行或平台支持证据。 |
| `onboarding.png`             | Windows 安装版的欢迎设置页，来自构建 `7728c46ec35959457648319d0391660b134f2b6d` 的隔离验证。                                |
| `doctor.png`                 | 同一 Windows 构建的 Doctor 页，展示隔离项目与当时观察到的运行环境 / AI 服务就绪状态。不代表所有平台、版本或服务均就绪。     |

发布前已目视检查：没有用户提示词、Agent 输出、源代码、绝对个人路径、原生会话 ID、凭据或私钥。PNG 不含文本元数据块。截图中的版本和检查时间只描述当次观察。

后续替换图片时，应使用隔离项目，并重新检查画面、图片元数据和能力描述。不要直接引用本地绝对路径、提交整个验证目录，或把演示数据标成真实执行。

CodeTether 标志引用仓库已有的 `apps/desktop/app-icon.svg`，未另行设计品牌资产。
