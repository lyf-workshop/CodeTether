# @codetether/web

React web/PWA surface for CodeTether.

Phase 1A adds only the development-only `/__ui` component showcase on top of the application bootstrap and shared styling integration. It is not a product route. Product layouts and screens remain out of scope until the next accepted phase and must follow Figma.
