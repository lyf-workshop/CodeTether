{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The CodeTether Web UI can select one Project directory and receives no shell or filesystem access.",
  "windows": ["main"],
  "permissions": ["allow-project-directory-picker"]
}
